// Inventory.cpp
#include "../../include/data_models/Inventory.h"
#include <iostream>

void Inventory::addItem(const std::string& itemName, int quantity) {
    // Add the item to the inventory or update its quantity if it already exists
}

void Inventory::removeItem(const std::string& itemName, int quantity) {
    // Remove the specified quantity of the item from the inventory
}

void Inventory::listItems() const {
    // List all the items in the inventory with their quantities
}
